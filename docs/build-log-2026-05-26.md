# omakase Plus MVP — Build Log

> Started 2026-05-26, resumed 2026-05-28.

## Phase 0 — Setup + branch + secrets

- **Branch:** `plus-mvp` cut from `origin/main` at `a33a4cd` (post-favicon fix).
- **Dependencies:** `pyproject.toml` bumped to 0.4.0. Added: `argon2-cffi`, `cryptography`, `itsdangerous`, `python-multipart`, `jinja2`. Upgraded `httpx` to `httpx[http2]`.
- **Baseline tests:** 61 passed (unchanged from public main).
- **Secrets checklist:** `docs/plus-secrets-checklist.md` written. All secrets TODO — user must set in Vaultwarden.
- **Auto-degrade:** Proceeding with mocks for missing env vars. Leftover TODOs surface in Phase 7 ask.
- **Status:** ✅ Complete.

## Phase 1 — Schema + auth + middleware

- **Sub-agent A (schema):** 7 SQLite tables with FK cascades + 4 indexes. Migration runner applies `migrations/NN-*.sql` idempotently. Pydantic v2 models for all tables.
- **Sub-agent B (auth):** argon2id password hashing, signed session cookies (30-day expiry), signup/login/logout routes with IP rate limiting (5/min), `require_user` FastAPI dependency, seed-user CLI.
- **Integration:** Plus router conditionally mounted in `web/server.py` when `OMAKASE_PLUS_PRIVATE=true`. Public mode: `/plus*` routes don't exist.
- **Tests:** 18 new (7 schema + 11 auth). Total: 79 passed. Lint clean.
- **Status:** ✅ Complete.

## Phase 2 — Per-user secrets storage

- **Encryption:** AES-256-GCM via `cryptography.hazmat`. Master key derived from `OMAKASE_PLUS_MASTER_KEY` via HKDF-SHA256.
- **CRUD:** `store_secret` (upsert), `read_secret` (decrypt), `delete_secret`. Per-user isolation via FK.
- **Settings UI:** `/plus/settings` page for managing API keys/tokens. Values never echo back to the page.
- **Tests:** 12 new (encryption roundtrip, tampering, unicode, CRUD, upsert, isolation). Total: 91 passed. Lint clean.
- **Status:** ✅ Complete.

## Phase 3 — AniList write integration

- **OAuth:** PKCE Authorization Code flow. OAuth connect/callback/disconnect routes.
- **GraphQL:** `SaveMediaListEntry` mutation for Planning list. Token validity check + `with_valid_token` context manager.
- **Routes:** `/plus/integrations/anilist/connect`, `/callback`, `/disconnect`; `POST /plus/api/plan` with dedupe.
- **Docs:** `docs/anilist-oauth-setup.md` for user-side OAuth app registration.
- **Tests:** 29 new. Total: 120 passed. Lint clean.
- **Status:** ✅ Complete.

## Phase 4 — Overseerr request trigger

- **Client:** `OverseerrClient` — search, request_media, get_request_status via REST API.
- **Automation:** `trigger_request_after_plan` orchestrator: search by title, best-match heuristic, insert into overseerr_requests.
- **Routes:** `POST /plus/api/auto-request`, `GET /plus/integrations/overseerr/status`.
- **Tests:** 14 new. Total: 134 passed. Lint clean.
- **Status:** ✅ Complete.

## Phase 5 — Plus dashboard UI

- **Dashboard:** Server-rendered page with 6 sections: header, taste profile editor, recommendation runner, results cards (+Plan/+Request buttons), collapsible recent runs, planning queue with Overseerr status.
- **Routes:** `GET /plus/dashboard`, `POST /plus/dashboard/profile`, `POST /plus/dashboard/run`, `POST /plus/dashboard/plan`.
- **Tests:** 12 new. Total: 146 passed. Lint clean.
- **Status:** ✅ Complete.

## Phase 6 — Public site refactor

- **Copy refresh:** Hero subtitle → "Personal anime recommender. Bring your own LLM key. Nothing stored server-side."
- **CTA:** Primary button → "Try it with your API key". BYOK banner expanded.
- **Footer:** "Plus accounts are private and not currently open."
- **Gating verified:** 0 `/plus*` routes in public mode.
- **Waitlist removal:** Zero waitlist references in `src/`.
- **Tests:** 146 passed (no regressions). Lint clean.
- **Status:** ✅ Complete.

## Phase 7 — Public site deploy

🛑 **HARD HALT — awaiting user approval.** (see ask above)

