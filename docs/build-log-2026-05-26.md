# omakase Plus MVP — Build Log

> Started 2026-05-26, resumed 2026-05-28.

## Phase 0 — Setup + branch + secrets

- **Branch:** `plus-mvp` cut from `origin/main` at `a33a4cd` (post-favicon fix).
- **Dependencies:** `pyproject.toml` bumped to 0.4.0. Added: `argon2-cffi`, `cryptography`, `itsdangerous`, `python-multipart`, `jinja2`. Upgraded `httpx` to `httpx[http2]`.
- **Baseline tests:** 61 passed (unchanged from public main).
- **Secrets checklist:** `docs/plus-secrets-checklist.md` written. All secrets TODO — user must set in Vaultwarden.
- **Auto-degrade:** Proceeding with mocks for missing env vars. Leftover TODOs surface in Phase 7 ask.
- **Status:** ✅ Complete.

## Phase 1 — Schema + auth + middleware

